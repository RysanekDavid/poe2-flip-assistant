[Unit]
Description=Nightly PoE2 Flip SQLite backup

[Timer]
OnCalendar=*-*-* 03:15:00
RandomizedDelaySec=10min
# Catch up after downtime so a reboot across 03:15 doesn't silently skip a night.
Persistent=true

[Install]
WantedBy=timers.target
